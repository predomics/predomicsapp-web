
=== PredomicsApp has been deployed! ===

{{- if .Values.ingress.enabled }}

Access the application at:
  https://{{ .Values.ingress.host }}

{{- else }}

Get the application URL by running:
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "predomicsapp.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME 8000:{{ .Values.service.targetPort }}
  echo "Visit http://127.0.0.1:8000"

{{- end }}

Health check:
  curl http://127.0.0.1:8000/health

{{- if not .Values.secrets.existingSecret }}

IMPORTANT: The chart auto-generated secrets for this release.
To view them:
  kubectl get secret {{ include "predomicsapp.fullname" . }} -o yaml

For production, create your own secret and set secrets.existingSecret in values.yaml.
{{- end }}
