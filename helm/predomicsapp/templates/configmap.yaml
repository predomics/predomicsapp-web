apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "predomicsapp.fullname" . }}
  labels:
    {{- include "predomicsapp.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
